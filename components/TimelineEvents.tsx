export default function TimelineEvents() {

    //TODO: inital fetch of daily events, should be 7 indicating 7 days ahead

    //get the browser current date

    //const dates = []

    //for (let i = 0; i < 6; i++) dates.push(currentDate + i) <-- i is increment for day

    // render a map over the dates array, display a DailyEvents comp with date prop


    return (<><div></div></>)
}
